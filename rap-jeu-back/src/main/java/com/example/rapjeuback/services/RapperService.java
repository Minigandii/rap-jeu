package com.example.rapjeuback.services;

import com.example.rapjeuback.models.Rapper;
import java.util.List;
/*
public interface RapperService {
    @Autowired
    public RapperServiceImpl(RapperRepository rapperRepository) {
        this.rapperRepository = rapperRepository;
    }

    @Override
    public List<Rapper> getAllRappers() {
        return rapperRepository.findAll();
    }

    @Override
    public Rapper getRapperById(Long id) {
        return rapperRepository.findById(id).orElse(null);
    }

    @Override
    public Rapper saveRapper(Rapper rapper) {
        return rapperRepository.save(rapper);
    }

    @Override
    public void deleteRapper(Long id) {
        rapperRepository.deleteById(id);
    }
}*/
